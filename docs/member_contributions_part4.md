# Member Contributions — Phase 4 (Source Control & Azure Deployment)

## Chidubem Okoye — Lead (Phase 4)
- Created and configured the private GitHub repository; added all team members as collaborators.
- Authored the Phase 4 submission PDF and the video demonstration script.
- Built the full-stack scaffold now in the repository: FastAPI backend, React + TypeScript frontend, PostgreSQL and MongoDB database scripts, Dockerfiles, and `docker-compose.yml`.
- Wrote the Azure infrastructure-as-code (`infra/` Bicep templates) and both GitHub Actions workflows (`ci.yml` and `deploy.yml`).
- Debugged and verified the local Docker Compose stack end-to-end on macOS.
- Added repository governance files: `LICENSE`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`.

## Yvonne Onmakpo — Data & Documentation
- Contributed to the relational schema (`backend/database/schema.sql`) and seed data (`backend/database/seed.sql`) carried over from Phase 2.
- Participated in code review for the probability-scoring service and data-modeling decisions.
- Contributed edits to the README and earlier-phase reports.

## Tonika Devi Avanigadda — Frontend & Presentation Support
- Contributed to the React frontend styling and component layout carried over from Phase 3.
- Assisted with the five-minute demonstration video production.
- Reviewed and commented on pull requests during Phase 4.

## Collaboration Notes
All three members have write access to the private repository. Every non-trivial change during Phase 4 was submitted via pull request on a feature branch and reviewed by at least one teammate before merging into `main`. Database scripts, infrastructure templates, and documentation are all version-controlled alongside the application code.